<% provide(:title, "Ingreso")%>
<h1>Ingreso</h1>
<div class="row">
  <div class="span6 offset3">
    <%= form_for(:session, url: sessions_path) do |f| %>

      <%= f.label :email %>
      <%= f.text_field :email %>

      <%= f.label :contraseña %>
      <%= f.password_field :password %>

      <%= f.submit "Ingresar", class: "btn btn-large btn-primary" %>
    <% end %>

    <p>Eres nuevo? <%= link_to "Registrate ahora!", registro_path %></p>
  </div>
</div>
