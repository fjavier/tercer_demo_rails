<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :contenido, placeholder: "Crear nuevo micropost..." %>
  </div>
  <%= f.submit "Terminado", class: "btn btn-large btn-primary" %>
<% end %>