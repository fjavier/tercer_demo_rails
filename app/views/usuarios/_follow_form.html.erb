<% unless current_user?(@usuario) %>
  <div id="follow_form">
  <% if current_user.following?(@usuario) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>